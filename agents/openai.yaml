interface:
  display_name: "Task Workflow"
  short_description: "Manage task-scoped repo clones, docs, runtime bootstrap, review, publish, sync, next-stage reuse, and task URL portal actions"
  default_prompt: "Use $task-workflow to create, continue, review/codeview current task changes, consolidate tests, start the next stage in the same workspace, publish, sync, inspect task URLs and ports, serve the dev portal, complete, clean up, or prepare runtime for a multi-repo task under /Users/wuyongli/Documents/sg-project/_workspace."

policy:
  allow_implicit_invocation: true
