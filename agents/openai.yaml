interface:
  display_name: "Task Workflow"
  short_description: "Manage multi-repo task workspaces and verification scope"
  default_prompt: "Use $task-workflow to create or run a multi-repo task: continue, review/verify, prepare runtime, PR/publish/sync, switch stages, inspect portal, complete, or clean up."

policy:
  allow_implicit_invocation: true
